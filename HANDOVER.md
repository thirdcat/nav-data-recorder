# 인수인계 — 2026-08-14

이 파일은 세션이 길어져 넘기는 것이다. **읽고 나면 지워도 된다.** 오래 남길
내용은 `docs/POSE.md`, `docs/VLIO.md`, `eval/README.md`, `AGENTS.md` 에 들어가
있고, 여기에는 그 문서들이 담지 않는 것만 적는다 — 지금 무엇이 중간에 멈춰
있는지, 사람이 확인해줘야 하는 것이 무엇인지.

궤적 데이터는 리포 안에 있다(둘 다 gitignore): `traj/` 는 30 Hz ICP 추정과 ARKit
기준, `pi3traj/` 는 같은 세션의 5 Hz Pi3X 궤적이다. 다시 만들려면 GPU 를 한참
돌려야 한다.

세션 원본은 `~/nav_data` 에 있다. 앱 빌드는 CI 의 `build-unsigned.yml` 이 유일한
컴파일러다.

---

## 1. 야간 세션 — 오늘 새로 측정된 것

`2994fa`(19.4 m, depth 30 Hz 완주) 와 `7d3d52`(20.5 m, 18초에 발열로 depth 중단)
가 **같은 경로를 두 번 걸은 야간 세션**이다. 어두운 방에서 ARKit 이 흔들리므로
ARKit 을 심판으로 쓸 수 없고, 참조 없는 검사로만 판정한다 —
`eval/compare_tracks.py` 가 그 검사들을 낸다.

```
                     2994fa            7d3d52
                  ICP   ARKit       ICP   ARKit
  경로 길이 (m)  20.43  19.31       9.61  10.48
  끝점–시작점    0.356  0.416       7.49   8.27   (7d3d52 는 루프가 아니다)
  평면 두께 (cm)   5.9    3.9        1.1    2.0
  평면⟂중력 (°)   0.44   1.42       1.35   3.57
  point-to-plane  4.3mm  4.4mm      3.4mm  3.5mm
  둘의 불일치    중앙값 30.6 cm    중앙값 21.9 cm
```

**주의 셋.**

- **point-to-plane 잔차는 ICP 가 최소화하는 양이다.** 자기 채점이므로 증거로
  치지 마라. 편향 없는 것은 루프 폐합과 중력 수직성 둘이다.
- **경로 길이 비가 두 세션에서 뒤집힌다**(1.058 → 0.917). 한쪽이 계통적으로
  스케일이 틀린 것이 아니라 드리프트다. 실제 경로 길이는 **줄자로 한 번 재면**
  이 항목이 닫힌다.
- npz 의 `gravity` 는 ARKit 포즈에서 나온 것이라, ARKit 회전으로 되돌리면 반드시
  0°가 나온다. **ARKit 의 중력 자기일치는 항등식이고 측정이 아니다.**

사람만 답할 수 있는 것 하나: **2994fa 에서 출발점으로 돌아왔는가.** 돌아온 것이
맞으면 0.356 대 0.416 이 그대로 루프 폐합 판정이 된다.

---

## 2. 발열 — 가설이 아니라 측정이다

오늘 밤 여덟 세션 중 걸은 것은 둘뿐이고, 나머지는 스크린샷용이라 지웠다. 남은
둘에서 발열이 이렇게 나왔다.

```
  2994fa   시작 thermal=fair      35초 완주, depth 30 Hz 유지
  7d3d52   시작 thermal=fair      18초에 serious → "video and depth paused"
  (지움)   시작 thermal=serious   시작부터 카메라 정지, 16분 동안 IMU·GPS 만
```

배터리 75%, 시작 시 fair 여도 35초를 못 넘긴다. 우리 세션이 13~52초인데 프리뷰가
오래 떠 있으면 녹화 시작 시점에 이미 뜨겁다. **결론: 프리뷰는 RGB 만.**

설계 갈래가 하나 남아 있다. RGB 만이면 ARKit 을 아예 안 띄우고 평범한
`AVCaptureSession` 으로 프리뷰할 수 있어 훨씬 시원한데, 그러면 녹화 시작 시점에
ARKit 트래킹이 차가운 상태로 출발한다(로그의 `limited:initializing`, 약 1초).
**이 갈래는 아직 정해지지 않았다.**

---

## 3. 사람만 할 수 있는 확인

앱 UI 가 크게 바뀌었고 **컴파일만 통과한 것들이 남아 있다.** 가로로 30~40초
찍으면 한 번에 확인된다. `gh run download <run-id> -n NavDataRecorder-unsigned`,
서명 없는 ipa 라 AltStore/SideStore 로 재서명.

1. **가로 한 방향 고정이 의도한 동작인가.** `LandscapeRight` 하나만 남겼다.
   반대로 들면 UI 가 거꾸로 보인다. 되돌리려면 `App/Info.plist` 에
   `LandscapeLeft` 한 줄.
2. **기하 경고의 축 이름.** 벽을 1 m 안쪽에서 정면으로 보면 "sideways", 복도 축
   방향에 "forward", 바닥이 안 보일 때 "height". **여기가 제일 틀리기 쉽다** —
   `weakAxis` 가 depth 프레임(+Z forward, +Y down) 기준이라는 주석만 믿고 짰다.
3. **깊이 색상**이 유리·거울(무반환 마젠타)에서도 구분되는가.
4. **BEV 가 현재 세션만 그리는가.** 오늘 고친 것이다 — 아래 4절.

---

## 4. 오늘 고친 것 — 빌드 확인 필요

**BEV 가 이전 세션 궤적을 그리던 문제.** 프리뷰 때문이 아니라 두 군데가 새고
있었다.

- `ARRecorder.start()` 가 `_snapshot` 은 비우는데 누적기 `_track` 은 안 비웠다.
  여섯 번째 프레임에서 `_snapshot.track = _track` 이 이전 세션 전체(최대 900점)를
  다시 써넣는다. "00:01 인데 한 바퀴" 가 이것이다.
- `tick()` 이 `.recording` 에서만 도는데 `finishSession()` 이 `stats` 를 안
  비웠다. 그래서 녹화가 끝난 뒤 대기 화면에 지난 세션 경로가 남았다.

**아무도 Swift 를 컴파일할 수 없다.** CI 가 유일한 컴파일러이므로 앱 변경은
직렬로 병합하고 사이마다 빌드한다.

---

## 5. 다음 단계 — 3DGS

앱을 3DGS 스캐너로 쓸 수 있는지가 다음 목표다. 참조는 SplatKing 과 Scaniverse
둘이고, 백엔드는 서버다(둘 다 캡처와 학습이 분리돼 있다).

```
                  SplatKing        Scaniverse(Niantic)      우리
  역할            캡처 전용        캡처 + 온디바이스 처리   캡처 전용
  멀티 렌즈       1x+0.5x 동시     단일                     없음
  LiDAR           모드 하나        있으면 사용              30Hz depth + confidence
  내보내기        미디어+pose+CSV  splat PLY/SPZ, mesh 다수  npz/jsonl
  metric scale    명시 없음        캘리브레이션 보드 필요   구성상 metric
```

**여기가 우리 자리다.** SfM/photogrammetry 는 원리적으로 스케일이 없어서
Scaniverse 가 1:1 을 위해 보드를 요구한다. 우리는 LiDAR depth 가 미터라 스케일이
공짜다. LiDAR depth supervision 계열(DN-Splatter, lidar-gsplat)이 요구하는 것이
정확히 **depth + 픽셀별 confidence + 그것으로 만든 dense init** 이고 셋 다 이미
녹화하고 있다.

빠진 것 넷, 어려운 순서로:

1. **전역 맵 덤프** — 매핑은 하고 있는데 버리고 있다. `tools/depth_odometry.py`
   의 `LocalMap` 이 3 cm voxel 마다 포인트와 법선을 running average 로 융합하는데
   (KinectFusion 의 TSDF 갱신에서 SDF 만 뺀 것), `trim()` 이 카메라 6 m 밖을 매
   키프레임마다 버린다. 저장되는 것은 포즈뿐이다. **전역 맵은 trim 을 끄면
   나온다** — 오프라인이라 메모리는 문제가 아니다. 이것이 3DGS init 포인트
   클라우드와 같은 산출물이라 시각화와 학습 입력을 동시에 만족한다.
2. **포즈 confidence 의 눈금.** 원료는 이미 전부 npz 에 있다 — `inliers`,
   `conditioning`, `frame_conditioning`, `weakAxis`. 없는 것은 "conditioning
   0.02 가 몇 cm 오차인가" 라는 환산이다. 13세션과 ARKit 으로 오프라인에서 잰다.
3. **포맷 변환** — COLMAP/Nerfstudio 가 읽는 모양으로.
4. **이미지 품질** — 5 Hz, JPEG q=0.85, 야간 블러. `videoHDRAllowed=false` 와
   프레임별 `exposureDuration` 은 이미 있다.

**시각화는 오프라인만.** 온디바이스 매핑을 얹으면 위의 발열 예산에 더 얹는
것이고, 녹화 자체가 끊긴다. 앱 안에 넣는다면 녹화 중이 아니라 Sessions 탭에서
끝난 세션을 여는 형태여야 한다.

**ultra-wide MultiCam** 은 SplatKing 의 간판 기능과 같은 방향이다(커버리지).
ARKit 과 배타적이라 `AVCaptureMultiCamSession` 으로 가야 하고, 그러면 지금 공짜로
얻는 frame-perfect 동기가 timestamp 정렬로 바뀐다. ARKit 없는 포즈 소스가 ARKit
급이라는 결과가 그 교환을 받아들일 수 있게 만든 것이다.

---

## 6. 검토가 끝나지 않은 것

**`eval/fuse_rate.py`** — Pi3X(5 Hz 전역) 와 ICP(30 Hz 국소) 를 융합해 30 Hz
궤적을 만든다. 코덱스가 썼고 11세션 전부 통과로 보고했다. 코드는 깨끗한데
**수용 기준 둘 다 사실상 통과가 보장돼 있었다.**

- 앵커 기준은 항등식이다. 융합이 앵커 프레임에 Pi3X 포즈를 그대로 대입하고
  채점이 그 같은 배열을 자기 자신과 비교한다. `delta_cm` 이 반올림된 0.00 이
  아니라 **정확히 `0.0`** 이다.
- 앵커 사이 기준은 누적 드리프트하는 ICP 단독과의 비교라 거의 자동이다.

**빠진 대조군: ICP 없이 Pi3X 앵커만 보간하는 판.** 그것이 같은 숫자를 내면 이
작업이 증명한 것은 "30 Hz 로 출력된다" 뿐이고 ICP 의 국소 정확도라는 전제는
미검증으로 남는다. 같이 잴 것이 하나 더 있다: 끝점 보정을 **월드 원점 기준**으로
회전 보간한다(`fuse_rate.py` 의 `_interpolate_rigid` 를 왼쪽 곱). 원점에서 멀면
레버암으로 중간 프레임이 cm 단위로 밀릴 수 있는데 지금 ATE 가 3~8 cm 다. 왼쪽
앵커 위치를 중심으로 보정하는 판과 비교하면 바로 나온다. GPU 불필요.

---

## 7. 닫힌 것 — 다시 시도하지 말 것

전부 측정으로 닫혔고 근거는 `docs/POSE.md` 에 있다.

- **이미지를 잔차 항으로** — 참조 세 상태 × 가중치 스윕 전부 순손실
- **지도 삽입 게이트를 inlier 로** — 자기 입력을 파괴하며 폭주, 4.6배 악화
- **GPS** — 정확도가 걸은 거리의 1.6배, 20중 13세션에서 위치가 안 움직임
- **LoGeR 등 long-horizon 모델** — 단일 패스가 220프레임(44초)까지 되고 13중
  12세션이 그 안이다
- **루프 폐합을 수용 기준으로** — 13중 8에서 궤적이 나빠지는데 루프가 좋아진다

**철회된 결론 둘**: "Pi3X 스케일은 회전 지배에서 실패한다"(r=+0.705) 와 "두 실패
축이 독립". 둘 다 24프레임 윈도우의 성질이었고, 그 24는 사람이 고른 값이 아니라
16 GB GPU 가 고른 값이었다.

---

## 8. 일하는 방식 — 대가를 치른 것들

- **판정 기준을 결과 보기 전에 파일로 고정한다.** 그리고 그 기준이 *실패할 수
  있는지* 확인한다 — 6절이 그 교훈이다.
- **자기 대조군과 3자리까지 같은 숫자는 일어나지 않은 측정이다.**
- **추정기의 세계 프레임은 중력 정렬이 아니다.** 첫 카메라 자세에서 출발하므로
  바닥을 보고 찍으면 축별 범위가 있지도 않은 고도 변화로 읽힌다. 오늘 한 번 잘못
  읽었다.
- **GPU 는 한 번에 하나** (`flock`), **측정 하나에 프로세스 하나**, **호스트 간
  arm 비교 금지.** 같은 프로세스의 두 번째 forward 가 0.5~1.4% 다르다.
- **출력이 아니라 생성기를 고친다.**
- **삭제 전에 대상을 재본다.** 오늘 "제자리 세션"으로 지울 뻔한 것 중 하나가
  20.5 m 를 걸은 세션이었다.
