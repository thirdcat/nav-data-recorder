# 인수인계 — 2026-08-19

이 파일은 세션이 길어져 넘기는 것이다. **읽고 나면 지워도 된다.** 오래 남길
내용은 `docs/POSE.md`, `docs/3DGS.md`, `calib/README.md` 에 들어가 있고, 여기에는
그 문서들이 담지 않는 것만 적는다 — 지금 무엇이 중간에 멈춰 있는지, 사람이
확인해줘야 하는 것이 무엇인지.

**시작하는 곳은 `docs/POSE.md` 맨 앞의 표다.** 이번에 그 표가 크게 바뀌었다.

궤적은 리포 안에 있다(gitignore): `traj/` 는 30 Hz ICP 추정과 ARKit 기준,
`pi3traj/` 는 5 Hz Pi3X 궤적이다. 세션 원본은 `~/nav_data`. 앱 빌드는 CI 의
`build-unsigned.yml` 이 유일한 컴파일러다.

**GPU 는 로컬이 낫다.** RTX 4070 Ti SUPER 16 GiB 에 `eval/.venv` 가 이미 있다.
`.31`(RTX 4080)은 리포도 venv 도 없어서 시스템 python 을 쓰게 되고, `.183` 은
다른 사람이 46/49 GiB 를 쓰고 있었다. 참고로 두 환경에서 돌려본 결과는 경로
길이 소수점까지 재현됐다(torch 2.6 대 2.13, 4080 대 4070 Ti).

```bash
export PYTHONPATH=$PWD/eval/vendor/Pi3 HF_HUB_OFFLINE=1
export PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True   # 울트라와이드 arm 은 없으면 OOM
flock /tmp/nav_gpu_local.lock eval/.venv/bin/python eval/pi3_poseless.py ...
```

---

## 1. 지금 멈춰 있는 것 — 촬영이 필요하다

**남은 실마리 대부분이 새 세션을 요구한다.** 촬영 없이 갈 수 있는 자리는 2 절
끝에 남긴 arm 별 depth scale 갈림 하나다.

찍을 것:

- **중심부에 질감 있는 장면 몇 초.** 렌즈 간 정합 실패 10건 중 8건이 "중심
  매칭 부족"이었고 `f83f55` 는 그 이유로 통째로 빠졌다. 복도 정면처럼 가운데가
  빈 장면이 원인이다
- **알려진 크기의 물체를 알려진 거리에서** 몇 초. 체커보드면 더 좋다. 아래
  4절의 미해결 항목을 닫는 유일하게 남은 경로다
- **걸음이 있는 세션.** 빌드 109 로 찍힌 유일한 세션 `2d9844` 는 12 초에 1.1 m,
  0.095 m/s 로 거의 제자리다. 내부 파라미터가 측정값으로 실린 유일한 세션인데
  궤적 품질을 물을 수 없다

빌드 109 가 기기에 있으므로 이제 찍는 세션마다 `depth_calibration` 과 렌즈별
활성 시야가 들어온다. **지금 채점된 세 세션은 아직 공장값을 스케일해 쓴다.**

---

## 2. 이번에 뒤집힌 것 — 위치 결론

경로 교란을 제거하니 표가 바뀌었다. 한 걸음이 두 렌즈를 통과하는 세션 셋을
양쪽 arm 으로 채점했다. 근거는 `docs/POSE.md` 에 있다.

```
                     중력 잔차        표면 두께 cm      join 잔차
                     fitted 평균도    10cm    40cm      중앙값 cm
  d0f44f  wide          1.42          4.75    9.20        0.577
  d0f44f  ultra-wide    1.40          5.09   10.98        0.303
  d67f0a  wide          0.71 *        4.55   10.38        1.167
  d67f0a  ultra-wide    1.20 *        5.43   11.73        0.519
  15fbb3  wide          2.07          4.07    9.70        0.410
  15fbb3  ultra-wide    1.74          4.03   10.08        0.299

  ARKit 대조군     2.45 / 3.85 / 3.92
  * 양쪽이 아직 신뢰구간인 첫 10.1 초로 제한
```

- **위치 — wide 2승 1무.** 옛 "울트라와이드 3전 3승"은 울트라와이드 걸음 셋을
  *다른* wide 걸음 셋에 댄 것이었다
- **회전 — 갈린다.** 무승부 하나, 각 렌즈 하나씩
- **window 간 일치 — 울트라와이드 3전 3승.** 유일하게 넓은 렌즈가 이기는 행이고,
  임계값 교차가 아니라 연속량이다

무승부가 난 `15fbb3` 이 하필 첫 7 초가 0.57 m 책상 클로즈업이고 두 arm 의 경로
길이도 0.5 % 안에서 같다(다른 둘은 9 % 차이). 넓은 렌즈에 담을 먼 장면이 없는
조건이다.

**`d67f0a` 의 두께는 두 arm 의 파손 플래그를 지나서 채점됐다.** 위 표의 별표는
중력 열에만 붙어 있고 두께는 31 초 전체다. 둘 다 안 걸린 첫 10.1 초로 자르면
7.32 대 8.13 — 리드가 1.35 에서 0.81 로 줄지만 방향은 그대로고 여전히 읽을 값
위다. 나머지 두 세션은 자를 플래그가 없다.

**국소 일치는 좋은데 누적 위치는 나쁘다 — 기제를 찾았다.** join 잔차가 스케일을
흡수한 *뒤에* 계산된다(`pi3_poseless.py:478`). 그래서 "window 간 일치" 행은 모양에
대한 진술이고 체인 스케일에는 눈이 멀어 있다. 그 스케일은 실제로 흐른다 — 누적
인자가 wide 1.787 / 0.937 / 0.846, 울트라와이드 0.794 / 0.295 / 1.340 이고, window
자신의 depth 스케일로 예측하면 0.93~1.00 이다. **join 을 3전 3승 한 arm 의 체인
하나가 스케일의 70 % 를 잃는다.** 첫 window 뒤로는 아무것도 스케일을 붙잡지 않는다.

**그런데 되돌리는 것이 낫지 않다.** `eval/join_scale.py --anchor` 로 각 window 를
자기 depth 스케일로 돌려놓고 재채점하면 9.20→9.99, 10.98→10.60, 9.70→8.58,
10.08→10.72 — 부호가 갈리고 넷 중 셋이 0.7 cm 아래다. 그래서 `pi3_poseless` 는
안 고쳤다. 위치 결론의 *방향* 은 양쪽에서 살아남지만 **세션별 여백은 렌즈보다 이
자유도에 더 민감하다**(1.78→0.61, 0.38→2.14).

남은 것은 arm 별 depth scale 자체의 갈림(울트라와이드 1.05~1.15, wide 0.98~1.05)
이고, 이것은 여전히 설명이 없다. 다음에 볼 자리는 여기다.

---

## 3. 이번에 기각한 것

**재투영 교란.** 울트라와이드 arm 만 19.272 mm extrinsic 을 통과하므로 위치
결과가 렌즈가 아니라 그 변환을 재는 것일 수 있었다. 병진만 0/1/2 배로 훑으니
40 cm 셀이 0.29 / 0.05 cm 폭에서만 움직인다(분해능 0.7). **기선을 완전히 지워도
wide 와의 격차가 1.49 / 1.30 cm 로 남는다.** 위치 차이는 렌즈와 원뿔의 것이다.

병진만 훑은 이유: 0.462 도 회전이나 원뿔 매핑을 빼면 울트라와이드 arm 이
작동하는 이유를 빼는 것이라 절제가 아니라 전제 제거가 된다.

**"울트라와이드가 두 배 버틴다" — 철회.** 하루 동안 표에 있었다. join 하나가
바를 **0.012 cm 차(440분의 1)** 로 넘은 것이었고, 그 join 이 0.3 % 만 작았으면
wide 가 플래그 없이 완주해서 *더 견고한* 쪽으로 읽혔을 것이다. 검출기는 그대로
두고 여백을 찍게 했다.

두 파손의 원인도 서로 다르다 — wide 는 노출 계단(네 프레임에 휘도 0.27→0.75),
울트라와이드는 세션 2 위 회전(76.6 deg/s)의 모션블러. 그리고 **wide 가 끊긴 그
시점에 울트라와이드는 더 큰 노출 변화(7.0 배 대 3.7 배)를 겪고도 안 끊긴다.**

---

## 4. 열린 채로 남은 것

**19.272 mm 기선의 크기가 실촬영에서 회수되지 않는다.** 두 경로가 실패했다:

- 시차 회귀 — 8 쌍의 기울기가 예측의 0.10~1.49 배로 흩어지고 원인 미상 (VOID)
- 표면 두께 — 2 배 오차가 궤적에 안 나타난다. 즉 이 측정은 그 값에 **둔감**하다.
  기각도 검증도 못 한다

방향은 프로브의 네 패치로 확인된 채 남는다. 남은 경로는 1 절의 "알려진 크기,
알려진 거리" 촬영뿐이다.

**LiDAR 시야 숫자가 셋이다** — Diagnostics 74.6, 공장 캘리브 72.6, 활성 포맷
70.38. 쓸 것은 활성 포맷 값인데 나머지 둘이 설명되지 않았다.

---

## 5. 계측기 — 무엇을 믿고 무엇을 믿지 않는가

**중력 잔차** (`eval/gravity_residual.py`). 이번에 새로 썼다 — `docs/POSE.md` 의
3.19°/2.78° 표를 만든 코드가 커밋된 적이 없어서 재현도 재실행도 불가능했다.
ARKit 대조군이 raw 26~62 도에서 fitted 2.45~3.92 도로 떨어진다.

자기 통제가 내 구현의 결함을 잡았다. 교대 최적화를 항등에서 출발시키면
국소최소에 빠져 **참값이 0 인 합성 데이터에 3.88 도**를 돌려준다. multi-start 로
0.01 도. 그리고 무작위 축 주입이 0.64 배로 감쇠하는 것을 그냥 넘기지 않고 중력에
수직인 축으로만 주입하는 팔을 붙였다 — 거기선 0.87 배로 돌아오므로 감쇠가 결함이
아니라 기하임이 확인된다. `--self-test` 로 아무 때나 돌릴 수 있다.

**표면 두께** (`eval/surface_thickness.py`). 주입 교정을 통과했고 셀 크기가
천장과 바닥을 동시에 정한다. 이 데이터에서 1 cm 주입이 coarse 를 0.69, 2 cm 가
1.89 움직이므로 **0.7 cm 가 읽을 가치가 있는 최소 차이**다.

**`--lens` 를 반드시 줘라.** 안 주면 wide arm 궤적에 울트라와이드 프레임을
붙인다. 두 인덱스는 번호가 겹쳐서 예외가 안 나고 그럴듯한 값이 나온다 —
틀린 arm 5.72/17.08, 맞는 arm 4.75/9.20.

**렌즈 간 초점거리 비** (`eval/wide_fov_probe.py`). 합성 회수를 통과했고, 기기가
값을 적어주기 *전에* 사전 등록한 규칙으로 정답을 골랐다.

**스케일 계측기** (`eval/scale_meter.py`). 이번에 새로 썼다. 궤적에 균일 스케일을
훑으면 40 cm 두께에 최소가 생기고 그 자리가 depth 대비 궤적 스케일이다. **이미
맞다고 믿는 입력에 교정했다** — ARKit 기준 궤적에 1.000 을 돌려주고, ×1.15 / ×0.90
을 미리 걸면 1.149 / 0.901 을 회수한다. `docs/POSE.md` 가 "기준 없이 스케일을 채점할
방법이 없다"고 적어둔 자리를 절반 메운다.

**절반인 이유: 채점된 여섯 arm 에는 안 통한다.** 우물이 0.02~0.56 cm 로 0.7 아래고
둘은 최소가 사다리 끝이라 괄호에 안 갇힌다. 도구가 그 경우 값을 안 내놓고 거부한다.
ARKit 세션은 760 포즈, poseless arm 들은 93~159 포즈 — 밀도가 유력한 이유지만
확인 안 됨.

**join 스케일** (`eval/join_scale.py`). 계측기가 아니라 보고서다. 2 절 참고.

**재방문 드리프트 — 폐기.** `eval/revisit_drift.py` 는 남아 있지만 쓰지 마라.
알려진 5 cm 를 주입하면 45 cm 가, 20 cm 를 주입하면 32 cm 가 돌아온다.

---

## 6. 닫힌 것 — 다시 시도하지 말 것

**포즈**

- **울트라와이드 정류** — 보정이 네 세션 모두에서 +3.19 cm, 리샘플만 하면
  -0.31 cm. 비용이 보정 자체다. **자르지도 마라** — rectifier 의 102.5→96.3 도
  절단이 이득의 원천인 장면을 버린다
- **해상도를 올려서 얻으려는 시도** — 세 가지 방식으로 무관함이 확인됐다
- **depth 커버리지를 늘리려는 시도** — wide 의 depth 를 41 퍼센트로 잘라도
  -0.11 cm
- **depth ICP 를 포즈로** — 바닥이 1 m 두께다. 원인은 기하 퇴화이고, 렌즈를
  넓혀도 LiDAR 절두체는 wide 카메라 시야에 묶인다

**3DGS**

- **세션 병합** — 모든 예산에서 진다
- **흐린 프레임 버리기** — 오히려 1.19 dB 나빠진다. 커버리지가 블러보다 비싸다
- **기하 fitness 나 ratio 로 정합 판정** — 진짜와 거짓이 완전히 겹친다

---

## 7. 철회한 결론

- **"울트라와이드가 위치에서 이긴다"** — 경로 교란 없이 재측정하니 wide 가
  2 승 1 무다. 옛 표는 걸음이 렌즈와 함께 움직이고 있었다
- **"울트라와이드가 두 배 버틴다"** — 3 절
- **"울트라와이드가 위치에서 진다"** (그 전 버전) — 재방문 계측기가 교정에 실패
- **"수평 이방성은 한 쌍의 성질"** — 네 쌍 모두에서 3.6~7.8 배로 나왔다
- **"2be6a9 ← 02a524 는 진짜 쌍"** — 일주일 동안 다섯 쌍 표에 있었다
- **"울트라와이드 extrinsic 은 미지수"** — 없던 것이 아니라 안 읽은 것이었다.
  **0 은 "이것이 기준"이라는 뜻**이었고, 멀티캠 세션이 정확히 항등을 보고하면서
  측정으로 확인됐다
- **"공장 캘리브레이션을 이미지 크기로 스케일하면 된다"** — 활성 포맷이 교정된
  포맷이 아니면 틀린다. wide 는 실제로 69.53 도였다(가정 72.6).
  **울트라와이드의 성질이 아니라 아무도 안 보던 렌즈의 성질이었다**

---

## 8. 일하는 방식 — 대가를 치른 것들

- **판정 기준을 결과 보기 전에 파일로 고정한다.** 기선 절제에서 값을 했다 —
  중력 잔차는 세션마다 최선이 뒤집혔고, 미리 40 cm 셀 하나만 읽기로 하지
  않았으면 동의하는 세션을 골랐을 것이다
- **계측기를 아는 답에 먼저 돌린다.** 중력 스코어러의 자기 통제가 참값 0 에
  3.88 도를 잡아냈다. 그럴듯한 회전 오차처럼 보이는 숫자였다
- **설명되지 않은 감쇠를 그냥 넘기지 않는다.** 0.64 배를 "대충 맞네" 로 두지
  않고 눈먼 축을 제거한 팔을 붙이니 0.87 배가 나왔다 — 감쇠가 기하임이 확인된다
- **주입 통제가 무엇에 눈이 먼지 묻는다.** 시차 계측기의 합성 통제는 워프와
  회귀에 같은 depth 를 써서 가장 유력한 용의자를 볼 수 없었다
- **임계값을 아슬아슬하게 넘은 것은 측정이 아니다.** 440 분의 1 로 넘은 교차가
  하루 동안 표의 한 행이었다. 여백을 같이 찍어라
- **자유 파라미터를 맞춘 뒤의 잔차는 그 파라미터를 못 본다.** join 잔차가 "window
  간 일치" 행으로 며칠 서 있었는데, 그 join 이 흡수한 스케일은 0.295 까지 흘렀다.
  무엇을 흡수하고 재는 잔차인지 먼저 물어라
- **표의 한 행이 아무도 등록 안 한 선택에 렌즈보다 민감할 수 있다.** join 스케일을
  자유로 두느냐 마느냐가 세션별 여백을 1.78→0.61, 0.38→2.14 로 옮긴다
- **절제가 전제를 제거하지 않는지 본다.** 기선만 훑고 원뿔 매핑은 남겼다
- **레버가 실제로 움직였는지 확인한다.** 기선을 빼면 depth 가 평균 1.88 cm
  움직인다는 것을 GPU 전에 봤다
- **비례성은 교정이 아니다.** 이미 맞다고 믿는 입력에 돌려야 한다
- **0 이 "없음"인지 "원점"인지 구별되지 않으면 코드가 거짓말한다**
- **계기가 자기를 설명하게 만든다.** 세션이 어느 빌드로 어느 포맷에서 찍혔는지
  적지 않으면 그 답을 노트의 *부재* 로 추론하게 된다
- **촬영 동작과 계측기를 한 커밋에서 같이 바꾸지 않는다**
- **말없이 틀리는 도구를 조심해라.** `surface_thickness.py` 가 arm 을 섞어도
  예외가 안 났다. 죽는 도구보다 이쪽이 비싸다
- **단위 라벨을 의심한다** · **자기가 만든 부작용을 본다**
- **GPU 는 한 번에 하나**(`flock`), **측정 하나에 프로세스 하나**, **호스트 간
  arm 비교 금지**
- **출력이 아니라 생성기를 고친다**
