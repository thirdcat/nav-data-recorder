# 인수인계 — 2026-08-14

이 파일은 세션 하나가 너무 길어져서 넘기는 것이다. **읽고 나면 지워도 된다.**
오래 남길 내용은 전부 `docs/POSE.md`, `docs/VLIO.md`, `eval/README.md`,
`AGENTS.md` 에 들어가 있고, 여기에는 그 문서들이 담지 않는 것만 적는다 — 지금
누가 무엇을 하고 있는지, 무엇이 중간에 멈춰 있는지, 사람이 확인해줘야 하는 것이
무엇인지.

브랜치 `claude/repo-review-yplc1b`. 전부 push 됨.

궤적 데이터는 리포 안에 있다(둘 다 gitignore): `traj/` 는 13세션의 30 Hz ICP
추정과 ARKit 기준, `pi3traj/` 는 같은 세션의 5 Hz Pi3X 궤적이다. 스크래치패드에만
두면 세션이 지워질 때 같이 사라지고, 다시 만들려면 GPU 를 한참 돌려야 한다.

앱 빌드는 CI 의 `build-unsigned.yml` 이 유일한 컴파일러이고 **build 87 통과**
상태다.

---

## 1. 지금 돌고 있는 것

**codex (tmux `3:0.2`, gpt-5.6-luna max)** — 브리프는 리포 루트의
`brief_codex_fusion.md`(gitignore 됨). Pi3X(5 Hz 전역) 와 깊이 ICP(30 Hz 국소)
를 속도 축으로 융합해 30 Hz 궤적을 만드는 일이다. GPU 불필요, 순수 numpy,
`eval/fuse_rate.py` 새 파일 하나만 만든다. **커밋하지 말라고 했으니** 워킹트리에
결과가 남는다. 수용 기준은 브리프에 결과 보기 전에 못박아뒀다.

**옆 Claude 세션 (tmux `3:0.1`)** — `docs/VLIO.md` 전담. `tools/` 를 이 세션
내내 한 번도 건드리지 않았고 앞으로도 안 한다. 아티팩트
(`capture_quality.html`) 도 그쪽이 가져갔다 — **사진을 사람이 눈으로 골라
넣었으므로 `eval/build_vis.py` 로 재생성해서 게시하면 안 된다.** 자동 선택이
화장실·침실·사람 사진을 골랐고 대외 공개 페이지라 손으로 교체했다. 선택 목록은
그쪽 세션의 스크래치패드에 `shots_manifest.json` 으로 있다 — 세션이 지워지면
같이 사라지므로, 그 페이지를 다시 만들 일이 생기면 먼저 그쪽에 물어라.

그쪽에 조사 하나를 부탁해뒀고 아직 시작 전이다: **ARKit 을 녹화 없이 프리뷰로
돌릴 때의 발열 비용.** 사용자가 "녹화 전에도 RGB·depth 상태를 보고 싶다"고 했는데,
우리 세션이 13~52초로 짧아서 프리뷰가 오래 떠 있으면 녹화 시작 시점에 이미 뜨거울
수 있다. 그쪽이 제안한 기준이 좋다 — 절대 시간(내가 근거 없이 말한 "30초")이
아니라 **세션 길이의 배수**로 자르는 것.

**GPU** — 7777(dnn, 96 GB)은 다른 작업이 쓰는 중이라 **건드리지 말 것**. 8888
(`10.231.180.183`, RTX 6000 Ada 48 GB)이 우리 몫이고 환경이 서 있다. 접속은
`ssh 10.231.181.31` 에서 `ssh myeongcheol@10.231.180.183` 로 두 홉이다(내 키가
저쪽에 없어서 ProxyJump 는 안 되고 중첩 ssh 만 된다).

---

## 2. 사람만 할 수 있는 확인 — 이게 지금 가장 큰 병목이다

앱 UI 를 오늘 크게 바꿨는데 **컴파일만 통과했지 화면에서 확인된 것은 절반뿐**이다.
build 87 을 설치하고 가로로 30~40초 찍으면 나머지가 한 번에 확인된다.

받는 법: `gh run download <run-id> -n NavDataRecorder-unsigned`, 서명 없는 ipa 라
AltStore/SideStore 로 재서명. 앱에서 `0.1.0 (87)` 로 보인다.

확인할 것:

1. **가로 한 방향 고정이 맞는가.** 오늘 `LandscapeRight` 하나만 남겼다. 사용자가
   "가로에서 위아래 구분이 힘들어 뒤집혀 찍히는 경우가 있다"고 해서, 반대로 들면
   UI 가 거꾸로 보이게 만든 것이다. **의도한 동작인지 사람이 판단해야 한다.**
   되돌리려면 `App/Info.plist` 에 `LandscapeLeft` 한 줄 추가.
2. **프리뷰가 똑바로 나오는가.** 방향이 하나가 되면서 보정을 상수 `.up` 으로
   바꿨다. 이전 세로 스크린샷에서는 90° 돌아가 있었다. 깊이 이미지도 같은 버퍼라
   같이 맞아야 한다.
3. **기하 경고의 축 이름.** 벽을 1 m 안쪽에서 5~10초 정면으로 보면
   `Flat NN% — Depth cannot see sideways motion…` 이 떠야 한다. **여기가 제일
   틀리기 쉽다** — `weakAxis` 가 depth 프레임(+Z forward, +Y down) 기준이라는
   주석만 믿고 짰고, 이 저장소는 축 규약으로 이미 하루를 잃은 적이 있다. 벽 정면에
   "sideways", 복도 축 방향에 "forward", 바닥이 안 보일 때 "height" 가 나와야
   한다.
4. **깊이 색상이 읽히는가.** 마지막 스크린샷에서 고양이 윤곽이 선명했으니 대체로
   통과인데, 무반환(마젠타)이 실제로 생기는 장면 — 유리·거울 — 에서도 구분되는지는
   아직 못 봤다.
5. **BEV 의 `N.NN m from start` 가 줄어드는가.** 출발점으로 돌아오면 줄어야 한다.

찍은 세션을 `~/nav_data` 에 넣으면 화면에서 본 것과 `depth.jsonl` 의
`cond`/`weakAxis` 를 대조할 수 있다. 앱 0.1.64 이상이면 그 값이 파일에 들어간다.

---

## 3. 다음에 할 일, 우선순위 순

1. **위 확인 다섯 가지** — 사람이 있어야 하고, 나머지가 여기 걸려 있다.
2. **codex 의 융합 결과 검토** — 브리프의 수용 기준으로 판정. 통과하면
   `eval/` 에 커밋, 아니면 그것도 결과다.
3. **프리뷰 상시 표시** — 옆 세션 조사 결과가 나온 뒤. 설계를 정하는 쪽이라
   나중에 바꾸면 비싸다.
4. **ultra-wide MultiCam 캡처 경로** — 오늘로 막고 있던 것이 전부 풀렸다. ARKit
   없는 포즈 소스가 ARKit 급이고, 시야각은 넓을수록 유리하고, 참조 없이 쓸 검사가
   둘 생겼다. 남은 것은 캡처 경로 자체이고 앱 작업이다. ARKit 과 배타적이라
   `AVCaptureMultiCamSession` 으로 가야 하고, 그러면 지금 공짜로 얻는 frame-perfect
   동기가 timestamp 정렬로 바뀐다.

---

## 4. 오늘 닫힌 것 — 다시 시도하지 말 것

전부 측정으로 닫혔고 근거는 `docs/POSE.md` 에 있다.

- **이미지를 잔차 항으로** — 참조 세 상태 × 가중치 스윕 전부 순손실
- **지도 삽입 게이트를 inlier 로** — 자기 입력을 파괴하며 폭주, 4.6배 악화
- **GPS** — 정확도가 걸은 거리의 1.6배, 20중 13세션에서 위치가 안 움직임
- **LoGeR 등 long-horizon 모델** — 단일 패스가 220프레임(44초)까지 되고 13중
  12세션이 그 안이다. 필요해지는 조건은 44초 넘는 촬영이고, 그때도 첫 수단은
  전역 median 스케일이다
- **루프 폐합을 수용 기준으로** — 13중 8에서 궤적이 나빠지는데 루프가 좋아진다.
  ARKit 이 있는 동안은 ATE 를 쓴다

그리고 **철회된 결론 둘**: "Pi3X 스케일은 회전 지배에서 실패한다"(r=+0.705) 와
"두 실패 축이 독립". 둘 다 24프레임 윈도우의 성질이었고, 그 24는 내가 고른 값이
아니라 16 GB GPU 가 고른 값이었다. 메모리의
`a-constant-you-did-not-choose-is-a-variable` 가 그 건이다.

---

## 5. 일하는 방식 — 오늘 대가를 치른 것들

- **판정 기준을 결과 보기 전에 파일로 고정한다.** 가중치 스윕이 5/8 승에
  기하평균 0.946 으로 나왔는데, 기준을 나중에 정했으면 "좋은 가중치를 찾았다"로
  읽혔을 것이다. 실제로는 dose 효과의 세 번째 재현이었다.
- **자기 대조군과 3자리까지 같은 숫자는 일어나지 않은 측정이다.** 오늘 이 규칙이
  결함 셋을 잡았다.
- **GPU 는 한 번에 하나.** "메모리가 얼마 비었나"가 아니라 "이미 도는 게 있나"를
  본다(`flock`). 이걸 안 지켜서 측정 셋을 날렸다.
- **측정 하나에 프로세스 하나**, **호스트 간 arm 비교 금지.** 같은 프로세스의 두
  번째 forward 가 0.5~1.4% 다르고, 카드가 다르면 2% 다르다.
- **출력이 아니라 생성기를 고친다.** 아티팩트 HTML 을 직접 고쳤다가 재생성으로
  날아갈 뻔했다.
- **아무도 Swift 를 컴파일할 수 없다.** CI 가 유일한 컴파일러이므로 앱 변경은
  **직렬로 병합하고 사이마다 빌드**한다. 그래야 깨졌을 때 원인이 분명하다.

---

## 6. 오늘 앱에 들어간 것 (build 79 → 87)

전부 컴파일만 확인됐다.

- 가로 2단 레이아웃, 방향 하나로 고정
- 센서 설정을 한 줄 텍스트로 (`RGB 5Hz · Depth 30Hz · IMU 100Hz`)
- 경고를 쌓지 않고 가장 심한 하나만 한 줄로, 뒤집힘 경고를 그 자리로 승격
- 깊이 표시 두 개(조명·기하)를 한 줄로
- 카운터 격자 제거, 녹화 버튼 절반 높이
- **깊이 이미지를 처음으로 화면에 그림** — 2 Hz, 무반환은 램프에 없는 색,
  거리는 광도 단조 램프, 저신뢰는 채도만 낮춤. 캡션에 p10/median/p95 와 무반환
  비율
- BEV 경로 지도와 `N.NN m from start`

`eval/` 쪽은 `docs/POSE.md` 와 `eval/README.md` 를 보면 된다.
