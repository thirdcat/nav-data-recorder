# 인수인계 — 2026-08-19

이 파일은 세션이 길어져 넘기는 것이다. **읽고 나면 지워도 된다.** 오래 남길
내용은 `docs/POSE.md`, `docs/3DGS.md`, `calib/README.md` 에 들어가 있고, 여기에는
그 문서들이 담지 않는 것만 적는다 — 지금 무엇이 중간에 멈춰 있는지, 사람이
확인해줘야 하는 것이 무엇인지.

**시작하는 곳은 `docs/POSE.md` 맨 앞의 표다.** 울트라와이드 경로가 무엇을
사는지 한 화면에 있고, 아래 모든 절이 그 표의 근거다.

궤적은 리포 안에 있다(gitignore): `traj/` 는 30 Hz ICP 추정과 ARKit 기준,
`pi3traj/` 는 5 Hz Pi3X 궤적이다. 세션 원본은 `~/nav_data`. 앱 빌드는 CI 의
`build-unsigned.yml` 이 유일한 컴파일러다.

GPU 는 `.31` 워크스테이션을 **경유해야** 닿는다. 이 셸은 `.183` 에 직접 인증하지
못하므로 중첩 SSH 로 붙고, 긴 스크립트는 따옴표가 깨지니 base64 로 넘긴다.

```bash
ssh myeongcheol@10.231.181.31 "ssh myeongcheol@10.231.180.183 'echo <b64> | base64 -d | bash'"
```

---

## 1. 지금 멈춰 있는 것 — 데이터는 다 있고 아무도 채점하지 않았다

**두 렌즈 세션이 다섯 개 있고, 그중 어느 것도 궤적으로 만들어지지 않았다.**
이것이 다음 할 일이고, 사람 손은 필요 없다. GPU 만 있으면 된다.

```
  20260818-121827-15fbb3   19.4s   98 / 93     절반은 책상 클로즈업
  20260818-121905-d0f44f   22.6s  114 / 109    ← 좁은 공간용으로 이걸 써라
  20260818-121944-f83f55    8.6s   44 / 42     쓰지 마라 (아래)
  20260818-121954-d67f0a   31.6s  159 / 152    ← 유일한 진짜 복도
  20260818-160918-2d9844   ~12s    58 / 63     ← 빌드 109, 자기를 설명하는 첫 세션
```

`eval/pi3_poseless.py --lens wide` 가 두 번째 arm 을 낸다. **dry-run 까지만
검증됐고 GPU 실행은 한 번도 안 했다.** 그것이 첫 번째 할 일이다.

`d0f44f` 를 좁은 공간 대표로 골라라 — 65 % 프레임이 2 m 안이고, 텍스처가 가장
풍부하고(책등), 모션블러 최소, wide 드롭 1건. `d67f0a` 는 유일한 복도 통과지만
첫 1/3 이 평균 휘도 0.33(어두운 욕실·현관)이라 앞 8초는 잘라라. `f83f55` 는
44장뿐이고 상당 부분이 무늬 없는 흰 옷장 문이다 — 기하학적으로는 가장 좁지만
특징이 없다.

**옛 세션과 새 세션을 한 표에 섞지 마라.** `160918-2d9844` 만 활성 포맷의
시야를 알고, 나머지 넷은 wide 초점거리가 여전히 가정이다(72.6 을 쓰는데 실제는
69.5). 도구가 그때마다 어느 쪽을 썼는지 출력하니 그 줄을 보면 된다.

---

## 2. 이번에 닫힌 것

**두 렌즈 동시 녹화.** 판별식 두 줄이 다섯 세션 전부에 있다. 이제 한 걸음이
두 렌즈를 통과하므로, 지금까지 모든 렌즈 비교가 안고 있던 "경로와 렌즈가 함께
움직인" 교란이 구성상 사라진다.

**발열.** 안 끊겼다. 울트라와이드 프레임 간격이 415장 전부 200.0004 ms 로 분산
0, 0.5초 넘는 공백 0건, 첫 1/3 대 마지막 1/3 저하 없음. `system pressure cost
1.9852095` 는 8.6초짜리를 포함해 네 세션에 바이트 단위로 동일하다 — 살아있는
측정이 아니라 정적 능력치 판독이다. 넷 다 사용자가 멈춘 것이다.

**좁은 공간.** 두 곳에서 채워졌다. 위의 두 렌즈 세션들이 기존 세션보다 확연히
좁고(`docs/DATA_FORMAT.md` 의 p05–p95 0.57–5.64 m 대비), 별도로
`20260818-084331-92e31a` 가 **승강기 안**이다 — ARKit 포즈가 깨끗하고, 안에
있는 동안 승강기가 안 움직여서 월드 프레임이 온전하다. 다만 그건 wide 전용이라
렌즈 비교에는 못 쓴다.

**캘리브레이션이 활성 포맷의 것이 아니라는 문제.** 울트라와이드에서만 알려져
있던 것이 모든 렌즈에 해당한다는 게 드러났고, wide 는 실제로 69.53 도였다
(가정은 72.6). `calib/README.md` 와 `docs/POSE.md` 에 근거째 들어가 있다.
레코더가 이제 렌즈별 활성 시야와 depth 내부 파라미터를 직접 적으므로 다음부터는
추론할 자리가 없다.

---

## 3. 사람만 할 수 있는 확인

- **중심부에 질감 있는 장면을 몇 초** 넣어서 찍어달라. 렌즈 간 정합 실패 10건
  중 8건이 "중심 매칭 부족"이었고 `f83f55` 는 그 이유로 통째로 빠졌다. 복도
  정면처럼 가운데가 빈 장면이 원인이다
- **`d3e82d` 와 `57f29e` 는 옛 빌드다.** 이제 `manifest.appBuild` 유무로 한 번에
  갈린다. 노트 부재로 추론하지 않아도 된다

---

## 4. 열린 채로 남은 것 하나

**19.272 mm 기선의 크기가 실촬영에서 회수되지 않는다.** 깊이 의존 시차항을
8쌍에서 적합하면 기울기가 예측의 0.10~1.49배로 흩어지고, 각 적합의 자체
표준오차보다 훨씬 넓다. depth 시야를 65~106 도로 훑어도, 가장자리를 걷어내도,
반경을 좁혀도 안 움직인다.

합성 주입에서는 알려진 초점거리를 정확히 회수하므로 산수와 기하는 맞다. **다만
그 합성 통제는 워프와 회귀에 같은 depth 를 쓰므로 depth-영상 정합 오차에
구조적으로 둔감하다** — 가장 유력한 용의자를 그 통제가 볼 수 없다. 그래서
초점거리를 보고하지 않았다. 방향 확인(`calib/README.md` 의 네 패치)은 영향받지
않고 그대로 유효하다.

`eval/wide_fov_probe.py` 에 계측기가 있다. 다시 볼 생각이면 `160918-2d9844`
로 돌려라 — depth 내부 파라미터가 이제 공장값 스케일이 아니라 측정값이라
원인 후보 하나가 갈린다.

---

## 5. 계측기 — 무엇을 믿고 무엇을 믿지 않는가

참조가 없는 세션을 채점해야 해서 계측기를 만들었다. **셋은 교정을 통과했고
하나는 폐기했다.**

**중력 잔차** (`docs/POSE.md`). CoreMotion 이 기기 프레임에서 중력을 보고하므로
회전이 맞으면 모든 프레임의 중력이 한 방향으로 모인다. ARKit 세션에서는 이것이
항등이었지만 — `gravity` 필드가 ARKit 포즈에서 파생된다 — 멀티캠 세션에서는
`motion.jsonl` 이 추정기를 본 적 없는 계기라 진짜 독립이다.

기기에서 카메라로 가는 회전은 **가정하지 말고 적합해야 한다.** 처음에 포즈를
기기 프레임 중력에 바로 곱했다가 대조군을 포함해 모든 arm 이 나빠졌다.
`R C g` 는 월드 중력에 `R C Rᵀ` 를 적용한 것이라 `R` 에 의존하고 고정 규약은
상쇄되지 않는다.

**표면 두께** (`eval/surface_thickness.py`). 주입 교정을 통과했고 셀 크기가
천장과 바닥을 동시에 정한다. 10 cm 셀은 5 cm 까지 읽고 바닥 3.7, 40 cm 셀은
20 cm 까지 읽고 바닥 9.2 다. **하나로 전 범위를 못 덮으므로 도구가 둘을
보고한다.** 틀린 셀의 숫자는 틀렸다기보다 눈이 먼 것이다.

**렌즈 간 초점거리 비** (`eval/wide_fov_probe.py`). 합성 회수를 통과했고, 기기가
값을 적어주기 *전에* 사전 등록한 규칙으로 정답을 골랐다. 9쌍에서 s = 0.3176,
IQR 0.0038. 비율만 보므로 어느 렌즈가 어긋났는지는 못 가른다 — 그 한계를 지킨
것이 이 계측기가 맞은 이유다.

**재방문 드리프트 — 폐기.** `eval/revisit_drift.py` 는 남아 있지만 쓰지 마라.
알려진 5 cm 를 주입하면 45 cm 가, 20 cm 를 주입하면 32 cm 가 돌아온다. 점수
표면이 평평하고 재방문 쌍이 0.32 로 "일부러 틀린 쌍" 대역 안에 있다. 40 프레임
떨어진 두 방문은 시점이 30 cm·20 도까지 벌어질 수 있고 그러면 폐색이 달라진다.

---

## 6. 닫힌 것 — 다시 시도하지 말 것

전부 측정으로 닫혔고 근거는 `docs/POSE.md` 와 `docs/3DGS.md` 에 있다.

**포즈**

- **울트라와이드 정류** — 보정이 네 세션 모두에서 +3.19 cm, 리샘플만 하면
  -0.31 cm. 비용이 보정 자체다. 그리고 울트라와이드가 wide 보다 이미 3~5배
  직선적이다. **자르지도 마라** — rectifier 의 102.5→96.3 도 절단이 이득의
  원천인 장면을 버린다
- **해상도를 올려서 얻으려는 시도** — 세 가지 방식으로 무관함이 확인됐다.
  640x480 이 1920x1440 을 회전에서 이겼고, 리샘플이 공짜이고, 각해상도 1.85배
  손실이 무료다. 열과 저장을 아끼는 데 쓸 수 있는 여유다
- **depth 커버리지를 늘리려는 시도** — wide 의 depth 를 41 퍼센트로 잘라도
  -0.11 cm. 프레임의 3분의 2 가 조건 없이 도착해도 모델이 감당한다
- **depth ICP 를 포즈로** — 연관을 바꿔도(map vs frame-to-frame) 바닥이 1 m
  두께다. 원인은 기하 퇴화이고, 렌즈를 넓혀도 LiDAR 절두체는 wide 카메라의
  시야에 묶인다 — 그 값은 포맷마다 다르니 세션의 `depth_calibration` 을 봐라

**3DGS**

- **세션 병합** — 모든 예산에서 진다. 동일 epoch 에서 1.60 dB, 동일 iteration
  에서 2.25 dB. 포토메트릭 재정합도 +0.29 dB 뿐이라 포즈가 원인이 아니다
- **흐린 프레임 버리기** — 오히려 1.19 dB 나빠진다. 버려진 24장 중 16장이
  30 cm·20 도 안에 대체 프레임이 없다. 커버리지가 블러보다 비싸다
- **기하 fitness 나 ratio 로 정합 판정** — 진짜와 거짓이 완전히 겹친다. 기본
  ratio 2.0 이 진짜 두 쌍을 기각하면서 거짓 다섯을 채택한다. 사진만이 가른다

---

## 7. 철회한 결론

문서에 적었다가 되물린 것들. 같은 함정을 다시 밟지 않으려면.

- **"울트라와이드가 위치에서 진다"** — 재방문 계측기가 교정에 실패해 표 전체를
  철회했다. 단서는 ARKit 절대 드리프트가 세 방에서 20.7/20.6/23.7 로 **너무
  일정한 것**이었다. 쌓이는 오차는 그렇게 가만있지 않는다
- **"수평 이방성은 한 쌍의 성질"** — 탐색 범위를 넓히니 네 쌍 모두에서 3.6~7.8배
  로 나왔다. 앞서 보고한 8~9 cm 는 좌표 하강이 범위 끝까지 달린 하한이었다
- **"2be6a9 ← 02a524 는 진짜 쌍"** — 일주일 동안 다섯 쌍 표에 있었다. 29 cm 를
  고친 뒤에도 0.78 이고 진짜 중 최악이 0.15 다
- **"울트라와이드 extrinsic 은 미지수"** — 없던 것이 아니라 안 읽은 것이었다.
  `MultiCamDepthProbe` 가 depth 프레임의 캘리브레이션에서 (0,0,0) 을 얻고
  그것을 "없음"으로 적었는데, **0 은 "이것이 기준"이라는 뜻**이었다. 멀티캠
  세션이 extrinsic 을 정확히 항등으로 보고하면서 이제 측정으로 확인됐다
- **"공장 캘리브레이션을 이미지 크기로 스케일하면 된다"** — 활성 포맷이 교정된
  포맷이 아니면 틀린다. 울트라와이드에서 먼저 걸렸고, wide 에서 한 주 더
  살아남았다. **울트라와이드의 성질이 아니라 아무도 안 보던 렌즈의 성질이었다**

---

## 8. 일하는 방식 — 대가를 치른 것들

- **판정 기준을 결과 보기 전에 파일로 고정하고, 그 기준이 실패할 수 있는지
  확인한다.** 렌즈 비 계측기가 이걸로 맞았다
- **계측기를 아는 답에 먼저 돌린다.** 자기 자신에 대해 0 이 나오는지, 알려진
  값을 주입하면 회수되는지. 재방문 계측기는 전자를 통과하고 후자에서 죽었다
- **주입 통제가 무엇에 눈이 먼지 묻는다.** 시차 계측기의 합성 통제는 워프와
  회귀에 같은 depth 를 써서, 가장 유력한 용의자를 구조적으로 볼 수 없었다.
  통과했다는 사실이 실데이터를 믿을 근거가 못 된다
- **비례성은 교정이 아니다.** 궤적에 k 를 곱하면 어떤 선형 추정기도 k 를
  돌려준다. 이미 맞다고 믿는 입력에 돌려야 한다
- **0 이 "없음"인지 "원점"인지 구별되지 않으면 코드가 거짓말한다.** extrinsic
  (0,0,0), 모든 오프셋이 실패한 스윕의 "0 에서 최소", 관측 불가한 스케일의 1.0
- **레버가 실제로 움직였는지 확인한다.** 게이트 통과율이 0 퍼센트면 "효과 없음"이
  아니라 "발동한 적 없음"이다
- **단위 라벨을 의심한다.** 잘 도는 두께 계측기를 미터를 센티미터로 찍어서 버릴
  뻔했다
- **자기가 만든 부작용을 본다.** co-observation 거부를 `score()` 안에 넣었더니
  비율의 분모가 무너져 트리에 `727708.0x` 가 찍혔다
- **계기가 자기를 설명하게 만든다.** 세션이 어느 빌드로 어느 포맷에서 찍혔는지
  적지 않으면, 그 답을 노트 문자열의 *부재*로 추론하게 된다. 한 줄이면 끝날
  것에 하루가 든다
- **촬영 동작과 계측기를 한 커밋에서 같이 바꾸지 않는다.** 다음 촬영이 무엇
  때문에 움직였는지 말을 못 하게 된다
- **GPU 는 한 번에 하나**(`flock`), **측정 하나에 프로세스 하나**, **호스트 간
  arm 비교 금지.** 그리고 `.183` 은 다른 사람도 쓴다 — 창 200 은 42 GiB 를
  요구해 OOM 이 났고 24 로 내려야 했다
- **출력이 아니라 생성기를 고친다.**
